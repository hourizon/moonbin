# MoonBit OSC2026 项目方向讨论整理

## 1. 比赛理解

本次比赛可以理解为一个 **MoonBit 开源生态项目贡献赛**。

它不是传统的算法竞赛，也不是只写一个能运行的小程序，而是希望参赛者从 **一个可发布的 MoonBit 包** 开始，做出一个公开、可复用、可维护的开源项目。

核心目标是：

> 为 MoonBit 生态补充基础库、工程工具、运行时能力或应用示例，让后来使用 MoonBit 的开发者有更多可直接复用的工具。

因此，项目需要尽量具备以下特点：

- 使用 MoonBit 作为主要实现语言；
- 项目仓库公开可访问；
- 功能边界清楚，不做空泛大项目；
- 有 README、示例、测试、CI；
- 可以被其他 MoonBit 项目复用；
- 后续有发布到 MoonBit 包生态的可能。

---

## 2. 官方推荐项目方向

根据赛事页面和项目方向截图，官方鼓励从 **一个可发布的 MoonBit 包** 开始。项目可以原创，也可以移植成熟生态中的开源库，但需要避免直接重复 MoonBit 生态中已经有且功能高度重合的成熟项目。

官方给出的方向大致分为四类：

| 方向 | 含义 | 示例 |
|---|---|---|
| 基础数据结构与算法 | 提供可复用的基础算法能力 | indexmap、bitmask、Graph 算法库、Pathfinding 工具库 |
| 工程基础设施与工具链 | 补齐 MoonBit 工程化开发常用工具 | 日志库、tracing 工具、构建工具、模板渲染器、benchmark 工具 |
| 系统能力与运行时框架 | 更底层的运行时、并发、格式处理能力 | deterministic simulation、寄存器分配库、序列化 / 反序列化工具、actor 框架 |
| 应用生态 | 可展示、可复用的生态项目 | Markdown to HTML、图表生成、游戏引擎绑定、数据库绑定 |

我们重点关注的是第三类：

> 面向特定格式的（反）序列化工具，类似 protobuf。

---

## 3. 序列化与反序列化是什么

**序列化** 指的是：

> 把程序里的对象、结构体或数据结构，转换成可以保存、传输的格式。

例如：

```text
User {
  name = "Hou"
  age = 18
}
```

序列化成 JSON 后可能是：

```json
{
  "name": "Hou",
  "age": 18
}
```

或者序列化成二进制后，变成一串字节。

**反序列化** 则是反过来：

> 把文件、网络数据、字符串或二进制字节重新解析成程序可以操作的数据结构。

它常见于：

- 配置文件读取；
- 前后端通信；
- 网络协议；
- 本地缓存；
- 游戏存档；
- RPC；
- 数据交换。

---

## 4. JSON 方向的讨论与调整

一开始我们讨论过做：

> MoonBit 轻量级 JSON 序列化 / 反序列化工具库

这个方向优点是：

- 容易理解；
- 容易展示；
- 适合入门；
- 和配置文件、网络通信、数据交换等场景相关。

但是后续发现，MoonBit 生态中已经有官方 JSON 能力。如果我们只是做一个普通的 JSON parse / stringify 工具，可能会和已有生态重复，项目价值不够突出。

因此，我们对 JSON 方向做过一次调整：

> 从“普通 JSON 库”调整为“JSON 配置解析与校验工具库”。

这个方向可以提供：

- 字段读取；
- 类型校验；
- 默认值；
- 错误路径提示；
- 格式化输出；
- 配置文件检查 CLI。

例如：

```text
$.database.port: expected Int, got String
```

这个方向比普通 JSON 库更工程化，但整体仍然偏应用工具。

---

## 5. protobuf 类方向是什么

后来我们重点讨论了官方示例中提到的：

> 面向特定格式的（反）序列化工具，类似 protobuf。

**protobuf** 全称是 **Protocol Buffers**，可以理解为一种高效的二进制序列化格式。

它和 JSON 的区别大致如下：

| 对比项 | JSON | Protobuf 类二进制格式 |
|---|---|---|
| 数据形式 | 文本 | 二进制 |
| 是否方便人直接阅读 | 方便 | 不方便 |
| 体积 | 较大 | 较小 |
| 解析速度 | 较慢 | 较快 |
| 是否需要提前定义结构 | 通常不需要 | 通常需要 |
| 常见场景 | Web API、配置文件 | 网络通信、RPC、本地缓存、高性能数据交换 |

完整 protobuf 很复杂，包括：

- `.proto` 语法解析；
- schema 定义；
- 字段编号；
- varint 编码；
- repeated / optional 字段；
- nested message；
- 代码生成；
- 版本兼容；
- 跨语言兼容。

因此，我们不建议直接写成：

> 完整实现 protobuf。

更稳妥的说法是：

> 借鉴 protobuf 的结构化二进制编码思想，实现一个面向 MoonBit 的轻量级二进制序列化与反序列化工具库。

这样既贴合官方方向，又能控制项目规模。

---

## 6. 最终推荐项目方向

最终建议将项目定为：

# moonbin：面向 MoonBit 的轻量级二进制序列化与反序列化工具库

项目定位：

> moonbin 是一个面向 MoonBit 生态的轻量级二进制序列化与反序列化工具库。项目借鉴 protobuf、bincode 等工具的结构化编码思想，设计一套简单、紧凑、可扩展的二进制格式，支持基础类型、字符串、字节数组、数组和结构化数据在 MoonBit 对象与字节序列之间进行双向转换。

这个项目属于官方方向中的：

> 系统能力与运行时框架 / 面向特定格式的（反）序列化工具。

---

## 7. 为什么最终选择 moonbin

相比 JSON 项目，moonbin 方向有以下优势：

| 方向 | 优点 | 问题 |
|---|---|---|
| 普通 JSON 库 | 容易理解，容易实现 | 可能和 MoonBit 已有 JSON 能力重复 |
| JSON 配置校验工具 | 工程实用，适合配置场景 | 更偏应用工具，系统性稍弱 |
| moonbin 二进制序列化工具 | 更贴合官方推荐方向，技术含量更高，差异化更明显 | 实现难度略高，需要认真设计格式 |

最终选择 moonbin 的理由：

1. **更贴合官方推荐方向**  
   官方明确给出了“面向特定格式的（反）序列化工具，类似 protobuf”这一例子。

2. **生态价值更清晰**  
   MoonBit 作为新兴语言，需要更多基础工程能力。二进制序列化库可以服务于网络通信、本地缓存、数据交换等场景。

3. **项目边界可控**  
   不做完整 protobuf，而是做轻量版自定义格式，可以在比赛周期内完成。

4. **技术含量适中**  
   比普通 JSON 工具更有含金量，但又不会像完整编译器、运行时、actor 框架那样过大。

5. **适合申报与展示**  
   可以清楚展示“对象 -> 字节序列 -> 对象”的过程，也能通过测试和错误处理体现工程质量。

---

## 8. 项目名称建议

可选名称：

| 名称 | 评价 |
|---|---|
| moonbin | 简短，容易记，突出二进制 |
| moonbit-bincode | 更正式，含义清楚 |
| moonbin-lite | 突出轻量，但略显普通 |
| moon-serializer | 范围偏泛，不如 moonbin 精准 |

推荐使用：

> moonbin

正式全称：

> moonbin：面向 MoonBit 的轻量级二进制序列化与反序列化工具库

---

## 9. 项目核心目标

moonbin 要实现的核心能力是：

```text
MoonBit 数据  ->  二进制字节
二进制字节    ->  MoonBit 数据
```

也就是：

```text
encode: value -> bytes
decode: bytes -> value
```

它可以支持：

- Bool；
- Int；
- Double；
- String；
- Bytes；
- Array；
- Object / Record；
- 嵌套结构；
- 错误处理；
- 测试和文档。

---

## 10. 最小可行版本

第一版不要追求完整 protobuf。最小可行版本可以控制在以下范围：

### 10.1 基础类型

支持：

```text
Bool
Int
Double
String
Bytes
```

### 10.2 复合类型

支持：

```text
Array
Object / Record
```

### 10.3 错误处理

定义错误类型，例如：

```text
DecodeError::UnexpectedEOF
DecodeError::InvalidType
DecodeError::InvalidLength
DecodeError::InvalidTag
DecodeError::TrailingBytes
```

### 10.4 文档和测试

提供：

- README；
- 格式说明文档；
- 基础示例；
- 正常编码解码测试；
- 非法输入测试；
- CI 检查、构建、测试流程。

---

## 11. 可能的 API 设计

基础编码接口示例：

```moonbit
encode_bool(true)
encode_int(123)
encode_string("hello")
```

基础解码接口示例：

```moonbit
decode_bool(bytes)
decode_int(bytes)
decode_string(bytes)
```

结构化数据示例：

```moonbit
let user = User::{ name: "Hou", age: 18 }
let bytes = encode_user(user)
let decoded = decode_user(bytes)
```

也可以提供更通用的内部表示：

```moonbit
enum BinValue {
  Bool(Bool)
  Int(Int)
  Double(Double)
  String(String)
  Bytes(Bytes)
  Array(Array[BinValue])
  Object(Array[(String, BinValue)])
}
```

然后支持：

```moonbit
encode(value : BinValue) -> Bytes
decode(bytes : Bytes) -> Result[BinValue, DecodeError]
```

---

## 12. 可能的二进制格式设计

可以设计一个简单的 TLV 格式：

> Type - Length - Value

即：

```text
类型标记 + 长度 + 数据内容
```

例如：

| 类型 | Tag |
|---|---|
| Null | 0x00 |
| Bool | 0x01 |
| Int | 0x02 |
| Double | 0x03 |
| String | 0x04 |
| Bytes | 0x05 |
| Array | 0x06 |
| Object | 0x07 |

示意：

```text
String("Hou")
= Tag(String) + Length(3) + bytes("Hou")
```

这种格式优点是：

- 结构清晰；
- 容易实现；
- 容易写文档；
- 容易做测试；
- 有一定扩展空间。

---

## 13. 项目申报书草稿

# moonbin 项目申报书

## 基本信息

项目名称：moonbin：面向 MoonBit 的轻量级二进制序列化与反序列化工具库

参赛者：侯瑞轩

联系方式：3219831300@qq.com

GitHub 仓库链接：待补充

Gitlink 仓库链接：待补充

项目方向：系统能力与运行时框架 / 面向特定格式的序列化与反序列化工具

是否为移植项目：否。本项目为原创项目，设计思路参考 protobuf、bincode 等二进制序列化工具的基本思想，但不移植其源码，也不以完整兼容 protobuf 为目标。

## 项目简介

moonbin 计划实现一个面向 MoonBit 生态的轻量级二进制序列化与反序列化工具库。项目将设计一套简单、紧凑、可扩展的二进制编码格式，支持 MoonBit 中常见基础类型与结构化数据在内存对象和字节序列之间进行双向转换。

相比 JSON 等文本格式，二进制序列化格式在体积和解析效率上具有优势，适合用于网络通信、本地缓存、数据交换、测试样例固化和运行时数据保存等场景。MoonBit 作为新兴语言，其生态仍需要更多基础工程工具支持。本项目希望补充 MoonBit 在结构化数据编码方向的基础能力，为后续 RPC、配置快照、二进制协议解析和跨模块数据交换等场景提供可复用组件。

## 核心功能范围

1. 设计 moonbin 二进制编码格式，明确类型标记、长度编码、字段编码和错误处理规则。
2. 支持基础类型的编码与解码，包括 Bool、Int、Double、String、Bytes 等常见类型。
3. 支持数组、键值对象或结构化记录的编码与解码，能够表示嵌套数据结构。
4. 提供统一 API，例如 encode_bool、encode_int、encode_string、decode_bool、decode_int、decode_string，以及面向结构化数据的编码辅助接口。
5. 提供 DecodeError 错误类型，覆盖 UnexpectedEOF、InvalidType、InvalidLength、InvalidTag、TrailingBytes 等常见错误场景。
6. 提供格式说明文档，解释 moonbin 二进制格式的设计方式、编码示例和兼容性考虑。
7. 提供 README 示例，覆盖基础类型编码、结构化对象编码、字节序列解码、错误处理和简单使用场景。
8. 提供 MoonBit 测试用例，覆盖正常编码解码、嵌套数据、边界值、非法输入和错误提示等情况。
9. 使用持续集成工具完成检查、构建和测试流程，保证项目可复现运行。
10. 在 GitHub 和 Gitlink 上保持同步更新，提交记录清晰、连续、可追踪。

## 实现计划

第一阶段完成项目初始化、MoonBit 工程结构搭建、README 草稿、基础类型编码与解码接口，以及初步测试用例。

第二阶段实现字符串、字节数组、数组和结构化对象的编码与解码，完善错误类型和边界检查。

第三阶段补充格式说明文档、使用示例、CI 流程、更多测试用例和异常输入测试，整理项目交付材料。

## 最终交付

项目最终将交付一个公开可访问的 MoonBit 开源仓库，包含完整源代码、README、格式说明文档、示例程序、测试用例和 CI 配置。项目以 MoonBit 为主要实现语言，目标是提供一个小而完整、可复用、可维护的二进制序列化与反序列化工具库。

---

## 14. 申报阶段需要注意的要求

根据你贴出的比赛规则，项目申报阶段需要准备：

- CCF 官网报名；
- 加入赛事群；
- 提交参赛信息；
- 提交项目申报材料；
- 一页左右 PDF 申报书；
- GitHub 仓库链接；
- Gitlink 仓库链接；
- GitHub 仓库需要有 10–20 个有效 commits；
- 不能通过空提交、无意义拆分、重复提交来凑 commit 数；
- Gitlink 仓库需要和 GitHub 同步。

申报通过后：

- 赛方会通过邮件通知；
- 通过可获得第一阶段 150 元启动支持；
- 不通过会说明原因；
- 可以根据拒绝原因调整后继续申报。

---

## 15. 开发与验收要求

项目开发阶段需要保持：

- 公开；
- 连续；
- 可追踪；
- 有工单、提交记录、更新日志；
- 有清晰的工程记录。

验收时需要满足：

- 项目以 MoonBit 为主要实现语言；
- GitHub 和 Gitlink 仓库公开可访问；
- 提交记录清晰；
- 源码结构清晰；
- 能完成申报中声明的核心功能；
- 提供 README；
- README 说明项目目标、安装方式、使用方法和示例；
- 项目可以复现运行；
- 使用持续集成工具；
- CI 覆盖检查、构建、测试流程。

---

## 16. 初期有效 commit 规划

为了满足申报阶段 10–20 个有效 commits 的要求，可以按真实开发过程拆分：

| commit | 内容 |
|---|---|
| 1 | 初始化 MoonBit 项目结构 |
| 2 | 添加 README 初稿 |
| 3 | 添加 LICENSE |
| 4 | 添加 moonbin 二进制格式草案 |
| 5 | 定义核心类型和错误类型 |
| 6 | 实现 ByteWriter 基础结构 |
| 7 | 实现 ByteReader 基础结构 |
| 8 | 实现 Bool 编码与解码 |
| 9 | 实现 Int 编码与解码 |
| 10 | 实现 String 编码与解码 |
| 11 | 添加基础类型测试 |
| 12 | 实现 DecodeError 错误处理 |
| 13 | 添加非法输入测试 |
| 14 | 添加结构化对象编码示例 |
| 15 | 补充 README 使用方法 |
| 16 | 添加 CI 配置 |

这些 commit 都是有实际内容的，不属于空提交或无意义拆分。

---

## 17. 项目风险与控制

### 风险一：范围过大

如果目标写成“完整实现 protobuf”，会涉及 schema 解析、代码生成、兼容性设计等复杂内容，难以在比赛周期内完成。

控制方式：

> 明确写成“受 protobuf 启发的轻量级二进制序列化工具”，不承诺完整兼容 protobuf。

### 风险二：和已有生态重复

如果只做 JSON parse / stringify，可能和 MoonBit 官方 JSON 能力重复。

控制方式：

> 转向二进制格式，做差异化的系统工具。

### 风险三：实现过于简单

如果只支持几个基础类型，没有错误处理、测试和文档，可能看起来像课程作业。

控制方式：

> 加入格式说明、错误类型、非法输入测试、CI、README 示例。

### 风险四：提交记录不规范

如果集中一次性提交，或使用空提交凑次数，可能不符合申报要求。

控制方式：

> 从项目初始化开始就按功能点进行真实、连续提交。

---

## 18. 最终建议

最终项目方向建议定为：

> moonbin：面向 MoonBit 的轻量级二进制序列化与反序列化工具库。

不要写成：

> 完整 protobuf 实现。

也不要回到：

> 普通 JSON 序列化 / 反序列化库。

最合适的表述是：

> 本项目借鉴 protobuf 的结构化二进制编码思想，但不追求完整兼容 protobuf，而是面向 MoonBit 生态实现一个小而完整、可复用、可测试、可维护的轻量级二进制序列化与反序列化工具库。

这样既贴合官方推荐方向，也能控制难度，还便于申报、开发和验收。
