# moonbin

moonbin is a lightweight binary serialization and deserialization library for MoonBit.

The project defines a compact custom binary format for common structured data and provides APIs for encoding MoonBit values into bytes and decoding bytes back into values.

